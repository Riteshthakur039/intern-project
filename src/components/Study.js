import { memo } from "react";

const Study = memo(() => {
  return (
    <b className="relative text-xl font-med text-darkslateblue text-left mq450:text-base">
      Study
    </b>
  );
});

export default Study;
