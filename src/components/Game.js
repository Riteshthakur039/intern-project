import { memo } from "react";

const Game = memo(() => {
  return (
    <div className="relative text-xl font-medium font-med text-dimgray text-left mq450:text-base">
      Game
    </div>
  );
});

export default Game;
